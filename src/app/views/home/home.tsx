import { Box } from '@mui/material'
import React from 'react'
import theme from '../../../core/theme/theme'
import { LoginLogo } from '../login/style'

const Home = () => {
  return (
    <Box sx={{ display: "flex",  backgroundColor: theme.COLORS.WHITE_100, flex: 1}}>
    </Box>
  )
}

export default Home